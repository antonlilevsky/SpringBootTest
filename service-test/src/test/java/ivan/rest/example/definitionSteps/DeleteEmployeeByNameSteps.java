package ivan.rest.example.definitionSteps;\n\nimport io.cucumber.java.en.Given;\nimport io.cucumber.java.en.When;\nimport io.cucumber.java.en.Then;\nimport static org.junit.Assert.*;\nimport ivan.rest.example.model.Employee;\nimport ivan.rest.example.service.EmployeeService;\nimport java.util.Map;\n\npublic class DeleteEmployeeByNameSteps {\n    private EmployeeService employeeService = new EmployeeService();\n\n    @Given(\"the following employees exist:\")\n    public void givenEmployeesExist(io.cucumber.datatable.DataTable dataTable) {\n        // Logic to add employees to the service\n        for (Map<String, String> row : dataTable.asMaps(String.class, String.class)) {\n            Employee employee = new Employee(row.get(\"name\"), row.get(\"passportNumber\"), row.get(\"education\"));\n            employeeService.addEmployee(employee);\n        }\n    }\n\n    @When(\"I delete the employee with name \"Sam\"\")\n    public void deleteEmployeeByName(String name) {\n        employeeService.deleteEmployeeByName(name);\n    }\n\n    @Then(\"the employee with name \"Sam\" should no longer exist\")\n    public void verifyEmployeeDeleted(String name) {\n        assertNull(employeeService.findEmployeeByName(name));\n    }\n}