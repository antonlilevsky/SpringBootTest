package ivan.rest.example.definitionSteps;\n\nimport io.cucumber.java.en.Given;\nimport io.cucumber.java.en.When;\nimport io.cucumber.java.en.Then;\nimport static org.junit.Assert.*;\n\npublic class DeleteEmployeeByNameSteps {\n    // Simulated database\n    private List<Employee> employees = new ArrayList<>();\n\n    @Given(\"the following employees exist in the system:\")\n    public void the_following_employees_exist_in_the_system(List<Map<String, String>> employeeData) {\n        for (Map<String, String> data : employeeData) {\n            Employee employee = new Employee(data.get(\"name\"), data.get(\"passportNumber\"), data.get(\"education\"));\n            employees.add(employee);\n        }\n    }\n\n    @When(\"I delete the employee with name {string}\")\n    public void i_delete_the_employee_with_name(String name) {\n        employees.removeIf(employee -> employee.getName().equals(name));\n    }\n\n    @Then(\"the employee with name {string} should not exist in the system\")\n    public void the_employee_with_name_should_not_exist_in_the_system(String name) {\n        assertFalse(employees.stream().anyMatch(employee -> employee.getName().equals(name)));\n    }\n}